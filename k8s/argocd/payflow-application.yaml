# ============================================
# ArgoCD Application - PayFlow GitOps
# ============================================
# Purpose: ArgoCD watches the k8s/ directory and auto-syncs
# When you commit changes, ArgoCD will deploy them

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: payflow
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/DevOps-Projects-by-Zee/payflow.git
    targetRevision: HEAD
    path: k8s
    # NOTE: No namespace override - let resources use their explicit namespaces
    # This prevents conflicts when resources span multiple namespaces (payflow, monitoring, argocd)
  destination:
    server: https://kubernetes.default.svc
    namespace: payflow
  syncPolicy:
    automated:
      prune: true  # Delete resources removed from Git
      selfHeal: true  # Auto-sync if drift detected
      allowEmpty: false
    syncOptions:
    - CreateNamespace=true
    - PrunePropagationPolicy=foreground
    - PruneLast=true

